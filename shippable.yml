language: node_js
node_js:
    - "stable"
build:
    pre-ci:
        - npm install
        - npm install -g firebase-tools
        - npm run-script build
    on_success:
        - firebase deploy --token $FIREBASE_TOKEN.
