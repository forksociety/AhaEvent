const INPUT_TYPES = {
  INPUT: 'input',
  TEXTAREA: 'text',
};

const DATE_TYPES = {
  DATE: 'date',
  DATE_TIME: 'date_time',
  RANGE: 'range',
  RANGE_TIME: 'range_time',
};

export default {
  INPUT_TYPES,
  DATE_TYPES,
};
